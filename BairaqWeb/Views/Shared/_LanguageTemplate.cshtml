<div id="dataTableLanguage" class="d-none">
    <div data-key="sSearch"><span class="input-group-addon"><i class="far fa-search"></i></span></div>
    <div data-key="sProcessing">@T("ls_Processing")...</div>
    <div data-key="sLengthMenu">@T("ls_Showmenuentries")</div>
    <div data-key="sZeroRecords">@T("ls_Nmrf")</div>
    <div data-key="sInfo">@T("ls_Ssteote")</div>
    <div data-key="sInfoEmpty">@T("ls_Stoe")</div>
    <div data-key="sInfoFiltered">(@T("ls_Ffmte"))</div>
    <div data-key="sInfoPostFix"></div>
    <div data-key="sUrl"></div>
    <div data-key="oPaginate">
        <div data-key="sFirst">@T("ls_First")</div>
        <div data-key="sPrevious">@T("ls_Previous1")</div>
        <div data-key="sNext">@T("ls_Next1")</div>
        <div data-key="sLast">@T("ls_Last")</div>
    </div>
</div>